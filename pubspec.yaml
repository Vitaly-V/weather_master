name: weathermaster
description: Flutter weather App with BLoC

version: 1.0.0+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  equatable: ^1.1.1
  http: ^0.12.0+4
  flutter_bloc: ^3.2.0
  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1

flutter:
  uses-material-design: true
  assets:
    - assets/
